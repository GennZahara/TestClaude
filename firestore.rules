rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // papers 컬렉션: 읽기만 허용 (쓰기는 Cloud Functions에서만)
    match /papers/{paperId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
